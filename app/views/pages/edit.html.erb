<% @page_title = "Create a Page" %>

<div>

<h2>Create Page</h2>

<%= form_for(@page, page_path(@page, :subject_id => @subject.id)) do |f|%>

    <%= render(:partial => "form", :locals => {:f => f}) %>

    <div>
        <%= f.submit("Update Page") %>
        <%= link_to("Back", pages_path(:subject_id => @subject.id)) %>
    </div>

<% end %>

</div>